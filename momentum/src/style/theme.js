const size = {
  mobile: "768px",
  tablet: "1024px",
};

const theme = {
  mainColor: "#222",
  subColor: "#666",
  mobile: `(max-width: ${size.mobile})`,
  tablet: `(max-width: ${size.tablet})`,
};

export default theme;
